# 변경 로그

## Unreleased

### 기반 구성

- Godot 4 프로젝트 기본 해상도와 가로 화면 설정을 추가했다.
- 빌드 산출물과 로컬 도구 파일을 Git 추적 대상에서 제외했다.
- 결정론적 전투 코어와 UI, Android 전송 계층을 분리하는 프로젝트 원칙을 문서화했다.

### 전투 코어

- 공유 팀 체력과 플레이어별 덱, 손패, 에너지 및 방어 상태 모델을 추가했다.
- 두 플레이어의 계획을 속도, 슬롯, 선택 순서로 결정론적으로 해결하도록 구현했다.
- 턴당 지원 카드 한 장 제한과 아군 방어, 에너지 전달, 팀 회복 효과를 추가했다.
- 초기 수직 슬라이스용 수호자, 기술자 및 공용 카드 9장을 추가했다.
- 상태 해시와 독립 실행 가능한 전투 코어 테스트를 추가했다.
- 4개 캐릭터별 24장과 공용 48장, 총 144장의 안정적 ID·등급·효과 카탈로그를 추가했다.
- 유물 24종, 소비 아이템 8종, 이벤트 18종과 3개 스테이지의 적·엘리트·보스·진보스 카탈로그를 추가했다.
- 플레이어별 36 내구도, 개별 방어, 동시 계획, 승리·패배·무승부 판정을 갖는 2인 결투 상태와 결정론적 엔진을 추가했다.
- 결투는 원정 강화 덱·유물·소비품을 배제하고 캐릭터별 표준 8장 덱을 사용하도록 분리했다.

### UI 수직 슬라이스

- 밝은 캐주얼 SF 디자인 토큰과 1280×720 가로 전투 화면을 추가했다.
- 공동 체력, 적 체력과 의도, 양쪽 캐릭터 상태, 연결 상태 및 손패를 한 화면에 배치했다.
- 카드 등급을 문자와 도형으로, 캐릭터 및 공용 카드 범위를 테두리 색으로 구분했다.
- 카드 선택, 에너지 및 지원 카드 제한, 동시 턴 해결을 조작 가능한 로컬 데모에 연결했다.
- 청록·주황의 협동 정체성을 담은 앱 아이콘과 수호자·기술자 캐릭터 일러스트를 제작해 적용했다.
- UI V2에서 6개 상단 탭을 단일 함선 메뉴로 통합하고, 16:9 궤도 전투 배경·좌우 캐릭터 스테이징·중앙 적 시각 요소로 전투 장면을 재구성했다.
- 손패 카드에 비용·등급·기하학 모티프·이름·효과의 시각 계층을 추가하고 선택 시 크기·테두리·상태 문구가 함께 변하도록 개선했다.
- 남은 에너지와 행동 확정을 하단 핵심 동선으로 분리하고, 플레이 모드를 두 개의 대형 비교 카드로 개편했다.
- UI 품질을 콘텐츠 수와 분리해 평가하는 100점 벤치마크 기준과 emulator-5556 증적을 추가했다.
- 모달 본문을 세로 스크롤 영역으로 바꿔 8구간 항로와 작은 화면에서 하단 조작이 잘리지 않도록 했다.
- 편성 화면에 현재 대원 초상을 추가하고, 모드 선택·보상·상점이 전투 카드와 동일한 시각 언어를 사용하도록 통합했다.
- Bluetooth 연결을 켜기·역할 선택·호환 코드 확인의 3단계로 표시하고 권한·페어링·비행기 모드 해결 안내를 빈 상태에 추가했다.
- 모션 줄이기, 진동 피드백, 선택 카드 발광을 기기별로 저장하는 화면·조작 설정을 추가했다.
- 전투·이벤트·결투 결과에 생존도, 진행도, 크레딧과 턴 지표를 추가하고 실제 보상이 있을 때만 카드 보상 CTA를 표시한다.
- 카드 보상과 상점에서 같은 카드가 중복 제시되지 않도록 결정론적 후보 제외 규칙과 회귀 검증을 추가했다.
- 보상 카드에 캐릭터 전용·공용 범위를 문자로 표시하고 획득 후 선택지를 제거한 전용 성공 화면으로 전환한다.
- 협동 이벤트를 P1/P2 선택 상태, 위험·안전 선택 카드, 합의 판정 안내가 있는 의사결정 화면으로 개편했다.
- 상점 구매 이력을 체크포인트에 저장하고 카드·유물·소비품의 중복 구매를 차단하며 구매 즉시 `판매 완료` 상태를 표시한다.
- 보스 진입 화면에 팀 내구도·열쇠·양쪽 크레딧과 교전 전 점검 안내를 추가해 비어 있던 전환 화면을 브리핑 화면으로 개편했다.
- emulator-5556에서 상점 구매 전후, 결투 승리, 3스테이지 보스, 런 완주 상태를 강제 재현하고 원래 STAGE 1/2 체크포인트로 복원했다.
- 안내 문구로만 존재하던 카드 제거 서비스를 덱 4열 비교, 제거 카드 재확인, 75 C 결제, 방문당 1회 및 최소 5장 제한까지 실제 상점 흐름으로 연결했다.
- 카드 효과 유형별 조준선·방패·십자·번개 아이콘과 효과 태그 문자를 추가해 손패에서 공격·방어·회복·에너지 역할을 색상 외 형태로 구분한다.
- 공격·방어·회복·에너지 4종의 SF 카드 아트를 추가하고 손패·보상·상점·카드 제거 화면이 같은 아트 매핑을 재사용하도록 카드 컴포넌트를 개편했다.
- 115% 큰 글씨와 카드 아트를 함께 표시할 때 하단 조작 문구가 잘리지 않도록 카드 내부 여백과 텍스트 계층을 다시 조정했다.
- P1/P2 초상에 상태 링과 `내 선택`·`계획 N장`·`확정 완료`·`상대 선택 중` 배지를 추가해 준비 단계를 색상 외 진행량·점·문자로 구분한다.
- 훈련·일반·정예·보스·진 최종 보스에 서로 다른 실루엣의 적 자산과 접근성 설명을 매핑해 조우 등급을 전투 화면에서 즉시 구분한다.
- 모션 줄이기에서도 공격 궤적·육각 방패·회복 링·에너지 링크를 고정 프레임으로 유지하고 접근성 설명에 효과 형태를 병기한다.
- 수호자·기술자·해커·강습병·공용 카드에 서로 다른 방벽·회로·글리치·슬래시·궤도형 프레임을 적용해 소유 범위를 색상 외 형태로 구분한다.
- 수호자·기술자·해커·강습병의 공격·방어·회복·에너지 전용 카드 아트 16종을 추가해 공용을 포함한 시각 프로필을 4종에서 20종으로 확장했다.
- 공격·방어·회복·에너지 공간 효과를 캐릭터별 16개 기하 연출과 고유 접근성 설명으로 확장했다.
- 23개 적 ID에 스테이지 문양·편대 점·등급 브래킷을 조합한 개별 식별 프로필을 적용하고 같은 정보를 접근성 이름에도 병기했다.
- 144개 카드 ID마다 고정된 64비트 전술 시길을 아트 띠에 추가해 같은 캐릭터·효과 아트를 공유하는 카드도 고유 형태로 식별되도록 했다.
- 키보드·스위치용 4px 포커스 링과 모달 포커스 트랩을 추가해 메뉴가 열린 동안 뒤쪽 전투 버튼으로 포커스가 이탈하지 않도록 했다.
- Android Back 요청의 자동 종료를 제어하고 같은 입력이 연속 전달되는 경우를 디바운스해 메뉴만 닫고 전투 포커스로 복귀하도록 했다.
- 핵심 레이블과 버튼 글자를 115% 확대하는 큰 글씨 모드를 추가하고 기기별 설정 저장, 동적 손패·모달 재적용을 구현했다.
- Android 시스템 글자 크기를 100~200% 범위에서 감지하고 100·110·120·130% 안전 배율로 HUD에 반영하며, 감지값과 적용값을 설정 화면에 표시한다.
- 함선 메뉴를 2열 3행과 전체 폭 설정 행으로 재배치해 130% 안전 배율에서도 7개 항목을 스크롤 없이 터치할 수 있게 했다.
- 선택 카드의 순번·대상·총비용·지원 제한을 행동 큐로 노출하고 확정 CTA에 선택 장수를 표시해 계획 오류를 확정 전에 확인하도록 개선했다.
- Godot의 SurfaceView UI를 Android 버튼·정적 텍스트 가상 접근성 노드로 변환하는 브리지를 추가해 카드 효과·대상·비용과 전투 상태를 스크린리더에 제공한다.
- 모달 전환 시 배경 접근성 노드를 제거하고 UIAutomator2 가상 ACTION_CLICK을 실제 Godot 버튼 동작으로 전달한다.

### Bluetooth 전송

- 게임 로직에서 분리된 세션 전송 인터페이스와 테스트용 메모리 루프백 구현을 추가했다.
- Godot Android Plugin v2 기반 Kotlin 플러그인 골격을 추가했다.
- Android 12 이상의 Nearby Devices 권한, RFCOMM 호스트·참가자 연결과 길이 프레임 메시지 입출력을 구현했다.
- 최대 메시지 크기, 연결 상태, 오류 큐 및 소켓 종료 처리를 추가했다.
- Android 플러그인 v2 표준인 EditorExportPlugin 패키징과 Gradle APK 포함 구성을 추가했다.
- 실행 시 Bluetooth 싱글턴 감지·어댑터 가용성·초기 상태를 로그와 상단 상태에 노출했다.
- 버전·순번·턴·상태 해시를 포함한 호스트 권한 세션 프로토콜과 재동기화 요청을 추가했다.
- Android 12 주변 기기 권한, Bluetooth 상태, 페어링 기기 목록과 호스트·참가 선택 로비를 추가했다.
- 호스트는 P1, 참가자는 P2 손패를 조작하고 실제 상대 계획을 기다리도록 전투 UI를 세션에 연결했다.
- 호스트 권위 결투 모드 선택, 비공개 행동 제출, 결투 상태 해시·스냅샷 동기화를 기존 Bluetooth 세션에 추가했다.
- 참가자 계획은 원문 대신 SHA-256 커밋을 먼저 보내고 호스트 행동 확정 뒤 nonce와 함께 공개해 선행 계획 열람을 차단했다.
- 커밋 해시와 참가자 nonce를 결투 상태와 분리된 로컬 체크포인트에 저장해 양쪽 앱 재시작 후에도 미완료 턴 공개를 재개한다.
- 결투와 커밋 체크포인트를 SHA-256 체크섬 봉투와 주·백업 교대 저장으로 변경해 기록 중단이나 JSON 손상 시 직전 정상본으로 복구한다.
- 동일한 체크섬 저널을 공용 저장 계층으로 추출하고 협동 원정 체크포인트에도 적용해 장기 런 진행 손실을 방지한다.
- 세션 종료 시 전송 신호와 상호 참조를 해제하고 앱 트리 종료에서 Bluetooth 연결을 닫아 재실행·종료 수명주기를 정리한다.
- 연결 직후 host/guest 역할과 144장 카드·원정 콘텐츠 SHA-256 지문을 교환하고 일치하기 전에는 게임 명령을 차단한다.
- 연결 표시를 VERIFYING·CONNECTED·VERSION MISMATCH로 구분하고 역할·콘텐츠 불일치 해결 방법을 한국어로 안내한다.
- 연결 화면에 두 기기가 육안 비교할 수 있는 12자리 호환 코드를 표시하고 15개 P0 실기기 E2E 판정표와 증적 양식을 추가했다.
- 전체 화면 modal scrim이 터치를 소비하도록 바꿔 메뉴가 열린 동안 뒤쪽 전투 카드나 확정 버튼이 오작동하지 않게 했다.
- Android RFCOMM 연결마다 generation ID를 부여해 이전 accept/read 스레드가 빠른 재연결로 생성된 새 소켓이나 상태를 닫지 못하게 했다.
- 실행 중 Bluetooth 권한 철회를 `permission_revoked`로 처리하고 최소 API 31에 불필요한 분기를 제거해 Android Lint 오류·경고를 해소했다.
- 결투 중 협동 전투·항로·편성·이벤트·소비품 명령을 호스트가 거부해 모드 상태가 교차 오염되지 않도록 격리했다.
- 연결이 끊겨도 살아 있는 호스트 세션을 재사용하고 현재 협동·결투 스냅샷을 다시 보내도록 재접속 복구 경로를 추가했다.
- 결투 ID·턴·내구도·손패·비공개 확정 계획을 별도 JSON 체크포인트에 저장해 앱 프로세스 재시작 후에도 복구하도록 추가했다.

### Android 실행

- Android 12 이상 Galaxy와 arm64 에뮬레이터 대상의 arm64-v8a 개발용 APK 내보내기 프리셋을 추가했다.
- 불필요한 x86_64 Godot 엔진을 제외해 개발 APK를 약 174MB에서 96MB로 축소했다.
- 에뮬레이터 UI 검증과 실제 갤럭시 Bluetooth 검증을 별도 단계로 분리했다.
- emulator-5556 실기기 캡처에서 발견된 세로 레터박스 문제를 수정하고 가로 방향을 명시했다.

### 런 진행

- 고정 시드 기반 3스테이지 맵과 8개 진행 단계, 개인 경로 및 공동 합류 지점을 추가했다.
- 각 플레이어 경로에 상점, 휴식, 엘리트 및 열쇠 도전이 반드시 등장하도록 보장했다.
- 캐릭터 전용 카드 2장과 공용 카드 1장으로 구성되는 전투 보상 생성기를 추가했다.
- 전용 카드 3장, 공용 카드 2장, 유물, 소비 아이템 및 카드 제거를 포함한 상점 재고를 추가했다.
- 스테이지 열쇠 3개에 따른 진보스 해금과 JSON 자동 저장·복구를 추가했다.
- 보상 수령, 상점 구매, 진행 단계와 체크포인트 저장을 통합한 런 코디네이터를 추가했다.
- 10,000개 시드에서 양쪽 경로의 필수 노드를 검증하는 불변성 테스트를 추가했다.
- 전투 상단에서 항로, 보상, 상점을 열고 획득·구매 결과를 즉시 저장하는 UI 흐름을 추가했다.
- 플레이어별 항로 선택, 공동 노드 합류, 양쪽 완료 후 진행과 열쇠 도전 성공 기반 해금을 추가했다.
- 스테이지 종료만으로 열쇠가 자동 지급되던 규칙 오류를 제거했다.
- 상점 유물·소비 아이템을 전체 콘텐츠 카탈로그와 연결하고 가격, 구매, 중복·소지 제한 및 자동 저장을 추가했다.
- 선택 노드에서 런 덱·공유 체력과 스테이지 적을 사용해 전투를 생성하고 승리 후에만 골드·열쇠·진행을 확정하도록 연결했다.
- Bluetooth 세션에 호스트 검증형 항로 선택과 런 스냅샷 동기화를 추가해 양쪽 기기의 항로·체크포인트 상태를 일치시켰다.
- 8개 항로 뒤 스테이지 보스를 강제하고, 3스테이지 보스·열쇠 게이트·진 최종 보스·런 완료 상태 전이를 연결했다.
- 휴식·이벤트와 전투가 함께 선택된 혼합 항로에서도 비전투 효과를 전투 승리 후 합산하도록 수정했다.
- 카드 보상을 전투당 플레이어별 1회로 제한하고, 상점 노드에 도착한 플레이어만 구매할 수 있도록 경제 접근 게이트를 추가했다.
- 18개 이벤트를 양쪽 선택·합의·절충·위험 판정이 있는 결정론적 협동 노드로 연결하고 전투 혼합 경로에서는 승리 후 이벤트를 진행하도록 변경했다.
- 구매한 소비품 8종을 방어·에너지·회복·피해·드로우·복제·회피 방벽·전투 에너지 강화 효과로 연결하고 사용 즉시 소비·저장·동기화하도록 구현했다.
- JSON 체크포인트에서 항로 슬롯 키가 문자열로 복원돼 전투 노드가 즉시 완료되던 문제를 정수 슬롯 정규화로 수정했다.
- 유물 24종을 전투 시작·턴 시작·카드 사용·지원 카드·피격 직전·전투 종료 트리거에 연결하고 효과와 발동 로그를 전투 스냅샷으로 동기화했다.
- 수호자·기술자·해커·강습병 4직업 편성과 직업별 8장 시작 덱, 중복 선택 차단, 호스트 권위 편성 동기화를 추가했다.
- 플레이 메뉴에서 협동 원정과 2인 결투를 전환하고, 비연결 상태에서는 한 기기를 번갈아 쓰는 2인 패스앤플레이 검증 흐름을 추가했다.
- 결투 종료 시 승자·최종 내구도·턴 수와 재대결·협동 전환 행동을 제공하는 결과 화면을 추가했다.
